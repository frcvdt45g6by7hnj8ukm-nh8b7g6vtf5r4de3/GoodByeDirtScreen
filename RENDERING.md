# 渲染问题核查（1.20.1 Forge）

## 已确认的问题

1. `TitleScreenMixin` 在原版标题画面已经绘制全景后再次调用模组背景事件，造成同一帧叠加第二次全景和额外旋转推进。
2. `ScreenMixin` 在原版 `renderBackground`/`renderDirtBackground` 结束后再绘制替换背景，实际保留了无用的原版绘制；两个入口还可能在同一帧重复触发。旧代码用 `partialTick` 判断是否同帧，但相邻帧可能有相同值，它不是可靠的帧编号。
3. 全景和后处理直接改变 OpenGL 状态时，没有先提交 `GuiGraphics` 的批次，也没有完整恢复主帧缓冲、深度、混合和着色器颜色，容易污染后续控件绘制。
4. 模糊 `PostChain` 每次绘制都关闭并重新创建；窗口尺寸也每帧重设。资源缺失时会继续解引用空对象。资源重载和客户端退出没有释放后处理和帧缓冲。
5. 模糊着色器把 RGB 除以样本数，却把累积 alpha 原样输出；半透明像素会得到错误 alpha。半径为零时旧权重表达式也存在除零风险。
6. 配置 JSON 内容为 `null`、列表字段为 `null` 或 JSON 损坏时，后续渲染路径可能空指针或无法回退。

## 修复后的行为

- 标题界面只使用原版的一次全景绘制。
- 其他允许替换的界面在原版背景入口开头绘制新背景并取消原调用；按真实 `GameRenderer` 帧清空身份集合，确保同一 `Screen` 每帧最多绘制一次。
- `excludeScreenList` 中的界面不取消原方法，因此保留原版背景。
- 后处理对象按需创建并缓存，仅在分辨率变化时调整尺寸；资源重载和客户端退出时释放。
- 全景、模糊和暗化之间主动提交 GUI 批次，并在后处理后重新绑定主帧缓冲和恢复渲染状态。
- 着色器按实际样本数平均完整 RGBA；配置损坏或缺字段时使用安全默认值。

## 验证

- `build` 通过，发布 jar 内包含 Mixin refmap。
- 资源重载使用 Forge 客户端事件释放后处理资源，客户端关闭时由 Minecraft Mixin 释放资源。
- 实际检查标题界面：全景只出现一层，没有黑屏或控件状态污染。
- 实际检查世界选择和选项界面：全景、模糊和暗化同时生效，控件正常显示和响应。
- `latest.log` 没有渲染、着色器或 Mixin 异常；离线开发账号的认证 401 与本修复无关。
